- name: Configure macbook
  hosts: all
  connection: local

  roles:
  - { role: terminal, tags: [ 'terminal' ] }
  - { role: atom_packages, tags: [ 'atom_packages' ] }
  - { role: snap_packages, tags: [ 'snap_packages' ] }
